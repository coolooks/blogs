---
import type { SiteMeta } from "@/data/types";
import { siteConfig } from "@/site-config";
import "../styles/global.css";

type Props = SiteMeta;

const { title, description, articleDate } = Astro.props;

const titleSeparator = "•";
const siteTitle = `${title} ${titleSeparator} ${siteConfig.title}`;
const canonicalURL = new URL(Astro.url.pathname, Astro.site);
---

<meta charset="utf-8" />
<meta name="viewport" content="width=device-width, initial-scale=1.0, shrink-to-fit=no" />
<meta http-equiv="X-UA-Compatible" content="IE=edge" />
<title>{siteTitle}</title>

<!-- Icons / Favicon -->
<link rel="icon" href="/blogs/favicon.ico" sizes="any" />
<link rel="icon" href="/blogs/icon.svg" type="image/svg+xml" />
<link rel="apple-touch-icon" href="/blogs/apple-touch-icon.png" />
<link rel="manifest" href="/blogs/manifest.webmanifest" />
<link rel="canonical" href={canonicalURL} />

<!-- Primary Meta Tags -->
<meta name="title" content={siteTitle} />
<meta name="description" content={description} />
<meta name="author" content={siteConfig.author} />

<!-- Theme Colour -->
<meta name="theme-color" content="" />

{
	articleDate && (
		<>
			<meta property="article:author" content={siteConfig.author} />
			<meta property="article:published_time" content={articleDate} />
		</>
	)
}

<!-- Sitemap -->
<link rel="sitemap" href="/blogs/sitemap-index.xml" />